spring:
  application:
    name: session2
  datasource :
    url : jdbc:postgresql://localhost:5432/clinic_db
    username : postgres
    password : 21mazen
  jpa:
    hibernate:
      ddl-auto: update
    show-sql : true